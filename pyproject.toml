[project]
name = "pace-planner-api"
version = "0.1.0"
description = "FastAPI service for the Pace study planner"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115.0",
  "psycopg[binary]>=3.2.0",
  "python-dotenv>=1.0.0",
  "sqlalchemy>=2.0.0",
  "uvicorn[standard]>=0.30.0",
]

[tool.uv]
package = false
